# 🚀 快速开始 - 文章同步

## 📝 概述

本项目使用自动化脚本将 `articles/` 目录中的文章同步到 `app/articles/` 目录，并动态生成 `_meta.ts` 文件，只包含实际存在的文章。

## ⚡ 快速测试

### 1. 测试环境

```bash
# 运行测试脚本，检查环境是否正常
node scripts/test-sync.js
```

### 2. 本地同步

```bash
# 运行同步脚本
node scripts/sync-articles.js
```

### 3. 检查结果

```bash
# 查看生成的文件
ls -la app/articles/

# 查看生成的 meta 文件
cat app/articles/_meta.ts
cat app/articles/volume-i-creation-and-fall/_meta.ts
```

## 🔄 GitHub Actions 使用

### 自动触发

以下情况会自动触发同步：

1. **定时触发**：每天凌晨 2:00 (UTC)
2. **文件变更**：推送到 `main` 分支且修改了 `articles/` 目录

### 手动触发

1. 进入 GitHub repository
2. 点击 **Actions** 标签
3. 选择 **"Sync Articles to App Directory"**
4. 点击 **"Run workflow"**
5. 点击绿色的 **"Run workflow"** 按钮

### Dry Run 模式

如果想预览变更而不提交：

1. 手动触发工作流
2. 勾选 **"Dry run"** 选项
3. 查看日志中的变更预览

## 📊 工作原理

```
articles/                          app/articles/
├── _meta.ts (7个卷)      ──→     ├── _meta.ts (只包含已同步的卷)
├── volume-i/                      ├── volume-i/
│   ├── _meta.ts (5篇)    ──→     │   ├── _meta.ts (只包含已同步的文章)
│   ├── article-1/ ✅     ──→     │   ├── article-1/ ✅
│   ├── article-2/ ❌              │   └── (article-2 不存在，不包含)
│   └── article-3/ ✅     ──→     └── article-3/ ✅
```

**关键特性**：
- ✅ 只同步实际存在的文章
- ✅ 动态生成 `_meta.ts`，避免404
- ✅ 保持原始格式和目录结构

## 🎯 常见场景

### 场景 1：添加新文章

```bash
# 1. 在 articles/ 中创建新文章
mkdir -p articles/volume-i-creation-and-fall/new-article
echo "# New Article" > articles/volume-i-creation-and-fall/new-article/page.md

# 2. 更新 _meta.ts
# 编辑 articles/volume-i-creation-and-fall/_meta.ts
# 添加: 'new-article': 'New Article Title',

# 3. 运行同步（本地测试）
node scripts/sync-articles.js

# 4. 提交并推送（自动触发同步）
git add .
git commit -m "Add new article"
git push
```

### 场景 2：渐进式发布

如果您想逐步发布文章：

1. 在 `articles/` 中只保留要发布的文章目录
2. 其他文章目录暂时删除或移到别处
3. 运行同步脚本
4. 生成的 `_meta.ts` 只包含已发布的文章

### 场景 3：批量同步

如果所有文章都准备好了：

1. 确保所有文章目录都存在
2. 确保每个目录都有 `page.md` 文件
3. 运行同步脚本
4. 所有文章都会被同步

## 🔍 验证同步结果

### 检查文件结构

```bash
# 查看同步的卷
ls app/articles/

# 查看某个卷的文章
ls app/articles/volume-i-creation-and-fall/

# 查看 meta 文件
cat app/articles/volume-i-creation-and-fall/_meta.ts
```

### 检查 meta 文件格式

生成的 `_meta.ts` 应该保持原始格式：

```typescript
export default {
  'article-1': '📖 Article 1 Title',
  'article-2': '✨ Article 2 Title',
}
```

**格式要点**：
- ✅ 单引号
- ✅ Trailing comma
- ✅ 2空格缩进
- ✅ 末尾空行

## 📱 在网站上查看

同步完成后，访问您的网站：

```
https://your-site.pages.dev/articles
```

导航菜单应该显示：
- 📚 圣经故事
  - 📖 Volume I: Creation and Fall
    - 🌟 1. Light and Chaos...
    - 2. Adam and Eve...
    - ...

## ⚠️ 注意事项

1. **备份数据**
   - 同步会覆盖 `app/articles/` 中的文件
   - 保留 `articles/` 作为源文件

2. **文章目录命名**
   - 目录名必须与 `_meta.ts` 中的 key 匹配
   - 使用小写字母和连字符

3. **必需文件**
   - 每个文章目录必须有 `page.md` 文件
   - 每个卷必须有 `_meta.ts` 文件

## 🐛 故障排除

### 问题：同步后文章不显示

```bash
# 检查文章目录是否存在
ls articles/volume-i-creation-and-fall/

# 检查是否有 page.md
find articles -name "page.md"

# 重新运行同步
node scripts/sync-articles.js
```

### 问题：Meta 文件格式错误

```bash
# 查看生成的 meta 文件
cat app/articles/_meta.ts

# 如果格式不对，检查源文件
cat articles/_meta.ts
```

### 问题：GitHub Actions 失败

1. 查看 Actions 日志
2. 检查错误信息
3. 确认 Node.js 版本（需要 v20）

## 📚 更多信息

详细文档请参考：
- [完整使用指南](./ARTICLE_SYNC_GUIDE.md)
- [GitHub Actions 工作流](.github/workflows/sync-articles.yml)
- [同步脚本](../scripts/sync-articles.js)

## 🎉 完成！

现在您可以：
1. ✅ 在 `articles/` 中管理文章
2. ✅ 自动同步到 `app/articles/`
3. ✅ 动态生成 `_meta.ts` 文件
4. ✅ 避免404错误
5. ✅ 渐进式发布内容

祝您使用愉快！ 🚀

